# 此文件仅供非 uv 用户参考，推荐使用 uv sync 管理依赖
#
# uv 用户请直接:
#   uv sync                     # 安装核心依赖
#   uv sync --extra baidu       # 额外安装百度千帆
#   uv sync --extra whisper     # 额外安装本地 Whisper
#   uv sync --extra dev         # 额外安装开发工具
#
# pip 用户:
#   pip install -r requirements.txt

feedparser>=6.0
requests>=2.28
pydub>=0.25
pyyaml>=6.0
dashscope>=1.20
oss2>=2.18
certifi>=2023.0  # SSL certificate bundle for websocket connections

# 可选：百度千帆
# qianfan>=0.4

# 可选：本地 Whisper
# faster-whisper>=1.0